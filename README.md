## Word Game
To run our app on docker (with the following docker file configurations):
docker-compose up --build (from the root directory)
</br>
to run locally, npm start in both client and server directories

